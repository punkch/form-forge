# High-contrast mode (accessibility) — PROMOTED

> **Promoted 2026-07-16 → `docs/specs/2026-07-16-1124-high-contrast-mode/`.**
> This proposal has moved into implementation. The full shaping and plan now
> live in the timestamped spec folder:
>
> - `plan.md` — full implementation plan (13 tasks: pure constants →
>   apply layer → no-FOUC script → ui store → hand-authored surface CSS →
>   generated accent AAA clamping → ratio-enforcing test → embed key →
>   Settings UI → design pass → forced-colors audit → docs).
> - `shape.md` — scope + the resolved decisions (orthogonal
>   `normal|high|system` preference, decoration reduced not just
>   strengthened, three-state Settings select, preview re-contrasted,
>   forced-colors as an audit).
> - `standards.md`, `references.md`, `user-guide.md`.
>
> This stub is kept only as a provenance breadcrumb.
