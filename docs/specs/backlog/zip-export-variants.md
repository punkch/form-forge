# ZIP export variants (XML + attachments / XLSX + attachments) — PROMOTED

> **Promoted 2026-07-16 → `docs/specs/2026-07-16-1120-zip-export-variants/`.**
> This proposal has moved into implementation. The full shaping and plan now
> live in the timestamped spec folder:
>
> - `plan.md` — full implementation plan (core `exportZip` variant parameter,
>   menu split, lockstep test updates).
> - `shape.md` — scope + the resolved decisions (single `zip` embed flag,
>   both download filenames suffixed `-xform`/`-xlsform`, ZIP entries
>   grouped last, full attachment set in both variants).
> - `standards.md`, `references.md`, `user-guide.md`.
>
> This stub is kept only as a provenance breadcrumb.
