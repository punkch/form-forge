# Parameter-specific help popovers in the properties panel — PROMOTED

> **Promoted 2026-07-16 → `docs/specs/2026-07-16-1121-parameter-help-tooltips/`.**
> This proposal has moved into implementation. The full shaping and plan now
> live in the timestamped spec folder:
>
> - `plan.md` — full implementation plan (`HelpPopover` param mode, unique
>   per-parameter testids, registry metadata audit as a separate commit).
> - `shape.md` — scope + the resolved decisions (registry stays the single
>   source of truth, generic `fieldHelp.parameters` copy removed, booleans
>   show "Default: false", options list kept even for Select inputs).
> - `standards.md`, `references.md`, `user-guide.md`.
>
> This stub is kept only as a provenance breadcrumb.
