# References for Spec 02

- `src/core/model/index-utils.ts` — `buildNodeIndex` provides the path/name
  maps the symbol table wraps.
- `xlsform-cheatsheet/relevance.md`, `constraints.md`, `list_lookups.md` —
  the real-world expression shapes the test tables mirror (`selected()`,
  `count-selected(.)`, `regex()`, `instance('x')/root/item[...]`).
- https://docs.getodk.org/xlsform/#referencing-questions — `${}` semantics.
- https://getodk.github.io/xforms-spec/ — XPath binding expression contexts.
- pyxform (Python) — behavioral reference for path-style rules; exact parity
  is pinned by the Spec 04 golden tests, not guessed here.
