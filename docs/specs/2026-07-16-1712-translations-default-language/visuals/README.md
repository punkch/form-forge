# Visuals

The user supplied three screenshots during shaping (2026-07-16). They were
provided inline in the conversation and are described here rather than stored:

1. **translation-grid-default-column** — the Translations dialog with a form
   declaring English (en) + French (fr). The grid shows String · Default ·
   English (en) 20/36 · French (fr) 20/36. Every "Default" cell is empty while
   en/fr carry the content — the column is pure noise for this (typical) form.
   The sidebar shows Languages, Add language, Default language (English (en)),
   and Show in editor.
2. **editing-language-dropdown** — the properties panel for a Date question:
   the "Editing language" select offers a blank value, "Default",
   "English (en)", "French (fr)" — three ways to say "not a real language"
   before the named ones.
3. **preview-language-picker** — the web-forms preview language menu offering
   exactly "English (en)" and "French (fr)". No "Default" entry: named itext
   languages are the only languages a client ever sees.
